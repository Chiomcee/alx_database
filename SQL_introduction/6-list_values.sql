-- list all rows of the table first_table from the hbtn_0c_0 database on your MySQL server
-- All fields should be printed
-- The database is passed as an argument to the mysql command

SELECT * FROM first_table;
